// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/menu"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/permission"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/permissionmenu"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PermissionMenuQuery is the builder for querying PermissionMenu entities.
type PermissionMenuQuery struct {
	config
	ctx            *QueryContext
	order          []permissionmenu.OrderOption
	inters         []Interceptor
	predicates     []predicate.PermissionMenu
	withPermission *PermissionQuery
	withMenu       *MenuQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PermissionMenuQuery builder.
func (pmq *PermissionMenuQuery) Where(ps ...predicate.PermissionMenu) *PermissionMenuQuery {
	pmq.predicates = append(pmq.predicates, ps...)
	return pmq
}

// Limit the number of records to be returned by this query.
func (pmq *PermissionMenuQuery) Limit(limit int) *PermissionMenuQuery {
	pmq.ctx.Limit = &limit
	return pmq
}

// Offset to start from.
func (pmq *PermissionMenuQuery) Offset(offset int) *PermissionMenuQuery {
	pmq.ctx.Offset = &offset
	return pmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pmq *PermissionMenuQuery) Unique(unique bool) *PermissionMenuQuery {
	pmq.ctx.Unique = &unique
	return pmq
}

// Order specifies how the records should be ordered.
func (pmq *PermissionMenuQuery) Order(o ...permissionmenu.OrderOption) *PermissionMenuQuery {
	pmq.order = append(pmq.order, o...)
	return pmq
}

// QueryPermission chains the current query on the "permission" edge.
func (pmq *PermissionMenuQuery) QueryPermission() *PermissionQuery {
	query := (&PermissionClient{config: pmq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(permissionmenu.Table, permissionmenu.FieldID, selector),
			sqlgraph.To(permission.Table, permission.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, permissionmenu.PermissionTable, permissionmenu.PermissionColumn),
		)
		fromU = sqlgraph.SetNeighbors(pmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMenu chains the current query on the "menu" edge.
func (pmq *PermissionMenuQuery) QueryMenu() *MenuQuery {
	query := (&MenuClient{config: pmq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(permissionmenu.Table, permissionmenu.FieldID, selector),
			sqlgraph.To(menu.Table, menu.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, permissionmenu.MenuTable, permissionmenu.MenuColumn),
		)
		fromU = sqlgraph.SetNeighbors(pmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PermissionMenu entity from the query.
// Returns a *NotFoundError when no PermissionMenu was found.
func (pmq *PermissionMenuQuery) First(ctx context.Context) (*PermissionMenu, error) {
	nodes, err := pmq.Limit(1).All(setContextOp(ctx, pmq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{permissionmenu.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pmq *PermissionMenuQuery) FirstX(ctx context.Context) *PermissionMenu {
	node, err := pmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PermissionMenu ID from the query.
// Returns a *NotFoundError when no PermissionMenu ID was found.
func (pmq *PermissionMenuQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = pmq.Limit(1).IDs(setContextOp(ctx, pmq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{permissionmenu.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pmq *PermissionMenuQuery) FirstIDX(ctx context.Context) int64 {
	id, err := pmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PermissionMenu entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PermissionMenu entity is found.
// Returns a *NotFoundError when no PermissionMenu entities are found.
func (pmq *PermissionMenuQuery) Only(ctx context.Context) (*PermissionMenu, error) {
	nodes, err := pmq.Limit(2).All(setContextOp(ctx, pmq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{permissionmenu.Label}
	default:
		return nil, &NotSingularError{permissionmenu.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pmq *PermissionMenuQuery) OnlyX(ctx context.Context) *PermissionMenu {
	node, err := pmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PermissionMenu ID in the query.
// Returns a *NotSingularError when more than one PermissionMenu ID is found.
// Returns a *NotFoundError when no entities are found.
func (pmq *PermissionMenuQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = pmq.Limit(2).IDs(setContextOp(ctx, pmq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{permissionmenu.Label}
	default:
		err = &NotSingularError{permissionmenu.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pmq *PermissionMenuQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := pmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PermissionMenus.
func (pmq *PermissionMenuQuery) All(ctx context.Context) ([]*PermissionMenu, error) {
	ctx = setContextOp(ctx, pmq.ctx, ent.OpQueryAll)
	if err := pmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PermissionMenu, *PermissionMenuQuery]()
	return withInterceptors[[]*PermissionMenu](ctx, pmq, qr, pmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pmq *PermissionMenuQuery) AllX(ctx context.Context) []*PermissionMenu {
	nodes, err := pmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PermissionMenu IDs.
func (pmq *PermissionMenuQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if pmq.ctx.Unique == nil && pmq.path != nil {
		pmq.Unique(true)
	}
	ctx = setContextOp(ctx, pmq.ctx, ent.OpQueryIDs)
	if err = pmq.Select(permissionmenu.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pmq *PermissionMenuQuery) IDsX(ctx context.Context) []int64 {
	ids, err := pmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pmq *PermissionMenuQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pmq.ctx, ent.OpQueryCount)
	if err := pmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pmq, querierCount[*PermissionMenuQuery](), pmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pmq *PermissionMenuQuery) CountX(ctx context.Context) int {
	count, err := pmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pmq *PermissionMenuQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pmq.ctx, ent.OpQueryExist)
	switch _, err := pmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pmq *PermissionMenuQuery) ExistX(ctx context.Context) bool {
	exist, err := pmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PermissionMenuQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pmq *PermissionMenuQuery) Clone() *PermissionMenuQuery {
	if pmq == nil {
		return nil
	}
	return &PermissionMenuQuery{
		config:         pmq.config,
		ctx:            pmq.ctx.Clone(),
		order:          append([]permissionmenu.OrderOption{}, pmq.order...),
		inters:         append([]Interceptor{}, pmq.inters...),
		predicates:     append([]predicate.PermissionMenu{}, pmq.predicates...),
		withPermission: pmq.withPermission.Clone(),
		withMenu:       pmq.withMenu.Clone(),
		// clone intermediate query.
		sql:       pmq.sql.Clone(),
		path:      pmq.path,
		modifiers: append([]func(*sql.Selector){}, pmq.modifiers...),
	}
}

// WithPermission tells the query-builder to eager-load the nodes that are connected to
// the "permission" edge. The optional arguments are used to configure the query builder of the edge.
func (pmq *PermissionMenuQuery) WithPermission(opts ...func(*PermissionQuery)) *PermissionMenuQuery {
	query := (&PermissionClient{config: pmq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pmq.withPermission = query
	return pmq
}

// WithMenu tells the query-builder to eager-load the nodes that are connected to
// the "menu" edge. The optional arguments are used to configure the query builder of the edge.
func (pmq *PermissionMenuQuery) WithMenu(opts ...func(*MenuQuery)) *PermissionMenuQuery {
	query := (&MenuClient{config: pmq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pmq.withMenu = query
	return pmq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PermissionID int64 `json:"permission_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PermissionMenu.Query().
//		GroupBy(permissionmenu.FieldPermissionID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pmq *PermissionMenuQuery) GroupBy(field string, fields ...string) *PermissionMenuGroupBy {
	pmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PermissionMenuGroupBy{build: pmq}
	grbuild.flds = &pmq.ctx.Fields
	grbuild.label = permissionmenu.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PermissionID int64 `json:"permission_id,omitempty"`
//	}
//
//	client.PermissionMenu.Query().
//		Select(permissionmenu.FieldPermissionID).
//		Scan(ctx, &v)
func (pmq *PermissionMenuQuery) Select(fields ...string) *PermissionMenuSelect {
	pmq.ctx.Fields = append(pmq.ctx.Fields, fields...)
	sbuild := &PermissionMenuSelect{PermissionMenuQuery: pmq}
	sbuild.label = permissionmenu.Label
	sbuild.flds, sbuild.scan = &pmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PermissionMenuSelect configured with the given aggregations.
func (pmq *PermissionMenuQuery) Aggregate(fns ...AggregateFunc) *PermissionMenuSelect {
	return pmq.Select().Aggregate(fns...)
}

func (pmq *PermissionMenuQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pmq); err != nil {
				return err
			}
		}
	}
	for _, f := range pmq.ctx.Fields {
		if !permissionmenu.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pmq.path != nil {
		prev, err := pmq.path(ctx)
		if err != nil {
			return err
		}
		pmq.sql = prev
	}
	return nil
}

func (pmq *PermissionMenuQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PermissionMenu, error) {
	var (
		nodes       = []*PermissionMenu{}
		_spec       = pmq.querySpec()
		loadedTypes = [2]bool{
			pmq.withPermission != nil,
			pmq.withMenu != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PermissionMenu).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PermissionMenu{config: pmq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(pmq.modifiers) > 0 {
		_spec.Modifiers = pmq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pmq.withPermission; query != nil {
		if err := pmq.loadPermission(ctx, query, nodes, nil,
			func(n *PermissionMenu, e *Permission) { n.Edges.Permission = e }); err != nil {
			return nil, err
		}
	}
	if query := pmq.withMenu; query != nil {
		if err := pmq.loadMenu(ctx, query, nodes, nil,
			func(n *PermissionMenu, e *Menu) { n.Edges.Menu = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pmq *PermissionMenuQuery) loadPermission(ctx context.Context, query *PermissionQuery, nodes []*PermissionMenu, init func(*PermissionMenu), assign func(*PermissionMenu, *Permission)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*PermissionMenu)
	for i := range nodes {
		fk := nodes[i].PermissionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(permission.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "permission_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pmq *PermissionMenuQuery) loadMenu(ctx context.Context, query *MenuQuery, nodes []*PermissionMenu, init func(*PermissionMenu), assign func(*PermissionMenu, *Menu)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*PermissionMenu)
	for i := range nodes {
		fk := nodes[i].MenuID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(menu.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "menu_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pmq *PermissionMenuQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pmq.querySpec()
	if len(pmq.modifiers) > 0 {
		_spec.Modifiers = pmq.modifiers
	}
	_spec.Node.Columns = pmq.ctx.Fields
	if len(pmq.ctx.Fields) > 0 {
		_spec.Unique = pmq.ctx.Unique != nil && *pmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pmq.driver, _spec)
}

func (pmq *PermissionMenuQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(permissionmenu.Table, permissionmenu.Columns, sqlgraph.NewFieldSpec(permissionmenu.FieldID, field.TypeInt64))
	_spec.From = pmq.sql
	if unique := pmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pmq.path != nil {
		_spec.Unique = true
	}
	if fields := pmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, permissionmenu.FieldID)
		for i := range fields {
			if fields[i] != permissionmenu.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pmq.withPermission != nil {
			_spec.Node.AddColumnOnce(permissionmenu.FieldPermissionID)
		}
		if pmq.withMenu != nil {
			_spec.Node.AddColumnOnce(permissionmenu.FieldMenuID)
		}
	}
	if ps := pmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pmq *PermissionMenuQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pmq.driver.Dialect())
	t1 := builder.Table(permissionmenu.Table)
	columns := pmq.ctx.Fields
	if len(columns) == 0 {
		columns = permissionmenu.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pmq.sql != nil {
		selector = pmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pmq.ctx.Unique != nil && *pmq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pmq.modifiers {
		m(selector)
	}
	for _, p := range pmq.predicates {
		p(selector)
	}
	for _, p := range pmq.order {
		p(selector)
	}
	if offset := pmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (pmq *PermissionMenuQuery) ForUpdate(opts ...sql.LockOption) *PermissionMenuQuery {
	if pmq.driver.Dialect() == dialect.Postgres {
		pmq.Unique(false)
	}
	pmq.modifiers = append(pmq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return pmq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (pmq *PermissionMenuQuery) ForShare(opts ...sql.LockOption) *PermissionMenuQuery {
	if pmq.driver.Dialect() == dialect.Postgres {
		pmq.Unique(false)
	}
	pmq.modifiers = append(pmq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return pmq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pmq *PermissionMenuQuery) Modify(modifiers ...func(s *sql.Selector)) *PermissionMenuSelect {
	pmq.modifiers = append(pmq.modifiers, modifiers...)
	return pmq.Select()
}

// Omit allows the unselect one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
// Example:
//
//	var v []struct {
//	  PermissionID int64 `json:"permission_id,omitempty"`
//	  MenuID int64 `json:"menu_id,omitempty"`
//	}
//
//	client.PermissionMenu.Query().
//	  Omit(
//	  permissionmenu.FieldPermissionID,
//	  permissionmenu.FieldMenuID,
//	  ).
//	  Scan(ctx, &v)
func (pmq *PermissionMenuQuery) Omit(fields ...string) *PermissionMenuSelect {
	omits := make(map[string]struct{}, len(fields))
	for i := range fields {
		omits[fields[i]] = struct{}{}
	}
	for _, col := range permissionmenu.Columns {
		if _, ok := omits[col]; !ok {
			pmq.ctx.Fields = append(pmq.ctx.Fields, col)
		}
	}

	sbuild := &PermissionMenuSelect{PermissionMenuQuery: pmq}
	sbuild.label = permissionmenu.Label
	sbuild.flds, sbuild.scan = &pmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// PermissionMenuGroupBy is the group-by builder for PermissionMenu entities.
type PermissionMenuGroupBy struct {
	selector
	build *PermissionMenuQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pmgb *PermissionMenuGroupBy) Aggregate(fns ...AggregateFunc) *PermissionMenuGroupBy {
	pmgb.fns = append(pmgb.fns, fns...)
	return pmgb
}

// Scan applies the selector query and scans the result into the given value.
func (pmgb *PermissionMenuGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pmgb.build.ctx, ent.OpQueryGroupBy)
	if err := pmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PermissionMenuQuery, *PermissionMenuGroupBy](ctx, pmgb.build, pmgb, pmgb.build.inters, v)
}

func (pmgb *PermissionMenuGroupBy) sqlScan(ctx context.Context, root *PermissionMenuQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pmgb.fns))
	for _, fn := range pmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pmgb.flds)+len(pmgb.fns))
		for _, f := range *pmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PermissionMenuSelect is the builder for selecting fields of PermissionMenu entities.
type PermissionMenuSelect struct {
	*PermissionMenuQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pms *PermissionMenuSelect) Aggregate(fns ...AggregateFunc) *PermissionMenuSelect {
	pms.fns = append(pms.fns, fns...)
	return pms
}

// Scan applies the selector query and scans the result into the given value.
func (pms *PermissionMenuSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pms.ctx, ent.OpQuerySelect)
	if err := pms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PermissionMenuQuery, *PermissionMenuSelect](ctx, pms.PermissionMenuQuery, pms, pms.inters, v)
}

func (pms *PermissionMenuSelect) sqlScan(ctx context.Context, root *PermissionMenuQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pms.fns))
	for _, fn := range pms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pms *PermissionMenuSelect) Modify(modifiers ...func(s *sql.Selector)) *PermissionMenuSelect {
	pms.modifiers = append(pms.modifiers, modifiers...)
	return pms
}
