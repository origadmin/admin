// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/menu"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/menupermission"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/permission"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// MenuPermission is the model entity for the MenuPermission schema.
type MenuPermission struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// MenuID holds the value of the "menu_id" field.
	MenuID int `json:"menu_id,omitempty"`
	// PermissionID holds the value of the "permission_id" field.
	PermissionID int `json:"permission_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MenuPermissionQuery when eager-loading is set.
	Edges        MenuPermissionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MenuPermissionEdges holds the relations/edges for other nodes in the graph.
type MenuPermissionEdges struct {
	// Menu holds the value of the menu edge.
	Menu *Menu `json:"menu,omitempty"`
	// Permission holds the value of the permission edge.
	Permission *Permission `json:"permission,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// MenuOrErr returns the Menu value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MenuPermissionEdges) MenuOrErr() (*Menu, error) {
	if e.Menu != nil {
		return e.Menu, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: menu.Label}
	}
	return nil, &NotLoadedError{edge: "menu"}
}

// PermissionOrErr returns the Permission value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MenuPermissionEdges) PermissionOrErr() (*Permission, error) {
	if e.Permission != nil {
		return e.Permission, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: permission.Label}
	}
	return nil, &NotLoadedError{edge: "permission"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MenuPermission) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case menupermission.FieldID, menupermission.FieldMenuID, menupermission.FieldPermissionID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MenuPermission fields.
func (mp *MenuPermission) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case menupermission.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			mp.ID = int(value.Int64)
		case menupermission.FieldMenuID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field menu_id", values[i])
			} else if value.Valid {
				mp.MenuID = int(value.Int64)
			}
		case menupermission.FieldPermissionID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field permission_id", values[i])
			} else if value.Valid {
				mp.PermissionID = int(value.Int64)
			}
		default:
			mp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MenuPermission.
// This includes values selected through modifiers, order, etc.
func (mp *MenuPermission) Value(name string) (ent.Value, error) {
	return mp.selectValues.Get(name)
}

// QueryMenu queries the "menu" edge of the MenuPermission entity.
func (mp *MenuPermission) QueryMenu() *MenuQuery {
	return NewMenuPermissionClient(mp.config).QueryMenu(mp)
}

// QueryPermission queries the "permission" edge of the MenuPermission entity.
func (mp *MenuPermission) QueryPermission() *PermissionQuery {
	return NewMenuPermissionClient(mp.config).QueryPermission(mp)
}

// Update returns a builder for updating this MenuPermission.
// Note that you need to call MenuPermission.Unwrap() before calling this method if this MenuPermission
// was returned from a transaction, and the transaction was committed or rolled back.
func (mp *MenuPermission) Update() *MenuPermissionUpdateOne {
	return NewMenuPermissionClient(mp.config).UpdateOne(mp)
}

// Unwrap unwraps the MenuPermission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mp *MenuPermission) Unwrap() *MenuPermission {
	_tx, ok := mp.config.driver.(*txDriver)
	if !ok {
		panic("ent: MenuPermission is not a transactional entity")
	}
	mp.config.driver = _tx.drv
	return mp
}

// String implements the fmt.Stringer.
func (mp *MenuPermission) String() string {
	var builder strings.Builder
	builder.WriteString("MenuPermission(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mp.ID))
	builder.WriteString("menu_id=")
	builder.WriteString(fmt.Sprintf("%v", mp.MenuID))
	builder.WriteString(", ")
	builder.WriteString("permission_id=")
	builder.WriteString(fmt.Sprintf("%v", mp.PermissionID))
	builder.WriteByte(')')
	return builder.String()
}

// MenuPermissions is a parsable slice of MenuPermission.
type MenuPermissions []*MenuPermission
