// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/menu"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/permission"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/permissionmenu"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// permission_menu.table.comment
type PermissionMenu struct {
	config `json:"-"`
	// ID of the ent.
	// field.primary_key.comment
	ID int64 `json:"id,omitempty"`
	// field.foreign_key.comment
	PermissionID int64 `json:"permission_id,omitempty"`
	// field.foreign_key.comment
	MenuID int64 `json:"menu_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PermissionMenuQuery when eager-loading is set.
	Edges        PermissionMenuEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PermissionMenuEdges holds the relations/edges for other nodes in the graph.
type PermissionMenuEdges struct {
	// Permission holds the value of the permission edge.
	Permission *Permission `json:"permission,omitempty"`
	// Menu holds the value of the menu edge.
	Menu *Menu `json:"menu,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// PermissionOrErr returns the Permission value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PermissionMenuEdges) PermissionOrErr() (*Permission, error) {
	if e.Permission != nil {
		return e.Permission, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: permission.Label}
	}
	return nil, &NotLoadedError{edge: "permission"}
}

// MenuOrErr returns the Menu value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PermissionMenuEdges) MenuOrErr() (*Menu, error) {
	if e.Menu != nil {
		return e.Menu, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: menu.Label}
	}
	return nil, &NotLoadedError{edge: "menu"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PermissionMenu) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case permissionmenu.FieldID, permissionmenu.FieldPermissionID, permissionmenu.FieldMenuID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PermissionMenu fields.
func (pm *PermissionMenu) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case permissionmenu.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pm.ID = int64(value.Int64)
		case permissionmenu.FieldPermissionID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field permission_id", values[i])
			} else if value.Valid {
				pm.PermissionID = value.Int64
			}
		case permissionmenu.FieldMenuID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field menu_id", values[i])
			} else if value.Valid {
				pm.MenuID = value.Int64
			}
		default:
			pm.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PermissionMenu.
// This includes values selected through modifiers, order, etc.
func (pm *PermissionMenu) Value(name string) (ent.Value, error) {
	return pm.selectValues.Get(name)
}

// QueryPermission queries the "permission" edge of the PermissionMenu entity.
func (pm *PermissionMenu) QueryPermission() *PermissionQuery {
	return NewPermissionMenuClient(pm.config).QueryPermission(pm)
}

// QueryMenu queries the "menu" edge of the PermissionMenu entity.
func (pm *PermissionMenu) QueryMenu() *MenuQuery {
	return NewPermissionMenuClient(pm.config).QueryMenu(pm)
}

// Update returns a builder for updating this PermissionMenu.
// Note that you need to call PermissionMenu.Unwrap() before calling this method if this PermissionMenu
// was returned from a transaction, and the transaction was committed or rolled back.
func (pm *PermissionMenu) Update() *PermissionMenuUpdateOne {
	return NewPermissionMenuClient(pm.config).UpdateOne(pm)
}

// Unwrap unwraps the PermissionMenu entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pm *PermissionMenu) Unwrap() *PermissionMenu {
	_tx, ok := pm.config.driver.(*txDriver)
	if !ok {
		panic("ent: PermissionMenu is not a transactional entity")
	}
	pm.config.driver = _tx.drv
	return pm
}

// String implements the fmt.Stringer.
func (pm *PermissionMenu) String() string {
	var builder strings.Builder
	builder.WriteString("PermissionMenu(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pm.ID))
	builder.WriteString("permission_id=")
	builder.WriteString(fmt.Sprintf("%v", pm.PermissionID))
	builder.WriteString(", ")
	builder.WriteString("menu_id=")
	builder.WriteString(fmt.Sprintf("%v", pm.MenuID))
	builder.WriteByte(')')
	return builder.String()
}

// PermissionMenus is a parsable slice of PermissionMenu.
type PermissionMenus []*PermissionMenu
