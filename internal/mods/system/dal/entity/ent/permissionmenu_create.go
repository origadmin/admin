// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/menu"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/permission"
	"origadmin/application/admin/internal/mods/system/dal/entity/ent/permissionmenu"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PermissionMenuCreate is the builder for creating a PermissionMenu entity.
type PermissionMenuCreate struct {
	config
	mutation *PermissionMenuMutation
	hooks    []Hook
}

// SetPermissionID sets the "permission_id" field.
func (pmc *PermissionMenuCreate) SetPermissionID(i int64) *PermissionMenuCreate {
	pmc.mutation.SetPermissionID(i)
	return pmc
}

// SetMenuID sets the "menu_id" field.
func (pmc *PermissionMenuCreate) SetMenuID(i int64) *PermissionMenuCreate {
	pmc.mutation.SetMenuID(i)
	return pmc
}

// SetID sets the "id" field.
func (pmc *PermissionMenuCreate) SetID(i int64) *PermissionMenuCreate {
	pmc.mutation.SetID(i)
	return pmc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (pmc *PermissionMenuCreate) SetNillableID(i *int64) *PermissionMenuCreate {
	if i != nil {
		pmc.SetID(*i)
	}
	return pmc
}

// SetPermission sets the "permission" edge to the Permission entity.
func (pmc *PermissionMenuCreate) SetPermission(p *Permission) *PermissionMenuCreate {
	return pmc.SetPermissionID(p.ID)
}

// SetMenu sets the "menu" edge to the Menu entity.
func (pmc *PermissionMenuCreate) SetMenu(m *Menu) *PermissionMenuCreate {
	return pmc.SetMenuID(m.ID)
}

// Mutation returns the PermissionMenuMutation object of the builder.
func (pmc *PermissionMenuCreate) Mutation() *PermissionMenuMutation {
	return pmc.mutation
}

// Save creates the PermissionMenu in the database.
func (pmc *PermissionMenuCreate) Save(ctx context.Context) (*PermissionMenu, error) {
	pmc.defaults()
	return withHooks(ctx, pmc.sqlSave, pmc.mutation, pmc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pmc *PermissionMenuCreate) SaveX(ctx context.Context) *PermissionMenu {
	v, err := pmc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pmc *PermissionMenuCreate) Exec(ctx context.Context) error {
	_, err := pmc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pmc *PermissionMenuCreate) ExecX(ctx context.Context) {
	if err := pmc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (pmc *PermissionMenuCreate) defaults() {
	if _, ok := pmc.mutation.ID(); !ok {
		v := permissionmenu.DefaultID()
		pmc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pmc *PermissionMenuCreate) check() error {
	if _, ok := pmc.mutation.PermissionID(); !ok {
		return &ValidationError{Name: "permission_id", err: errors.New(`ent: missing required field "PermissionMenu.permission_id"`)}
	}
	if v, ok := pmc.mutation.PermissionID(); ok {
		if err := permissionmenu.PermissionIDValidator(v); err != nil {
			return &ValidationError{Name: "permission_id", err: fmt.Errorf(`ent: validator failed for field "PermissionMenu.permission_id": %w`, err)}
		}
	}
	if _, ok := pmc.mutation.MenuID(); !ok {
		return &ValidationError{Name: "menu_id", err: errors.New(`ent: missing required field "PermissionMenu.menu_id"`)}
	}
	if v, ok := pmc.mutation.MenuID(); ok {
		if err := permissionmenu.MenuIDValidator(v); err != nil {
			return &ValidationError{Name: "menu_id", err: fmt.Errorf(`ent: validator failed for field "PermissionMenu.menu_id": %w`, err)}
		}
	}
	if v, ok := pmc.mutation.ID(); ok {
		if err := permissionmenu.IDValidator(v); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`ent: validator failed for field "PermissionMenu.id": %w`, err)}
		}
	}
	if len(pmc.mutation.PermissionIDs()) == 0 {
		return &ValidationError{Name: "permission", err: errors.New(`ent: missing required edge "PermissionMenu.permission"`)}
	}
	if len(pmc.mutation.MenuIDs()) == 0 {
		return &ValidationError{Name: "menu", err: errors.New(`ent: missing required edge "PermissionMenu.menu"`)}
	}
	return nil
}

func (pmc *PermissionMenuCreate) sqlSave(ctx context.Context) (*PermissionMenu, error) {
	if err := pmc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pmc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pmc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int64(id)
	}
	pmc.mutation.id = &_node.ID
	pmc.mutation.done = true
	return _node, nil
}

func (pmc *PermissionMenuCreate) createSpec() (*PermissionMenu, *sqlgraph.CreateSpec) {
	var (
		_node = &PermissionMenu{config: pmc.config}
		_spec = sqlgraph.NewCreateSpec(permissionmenu.Table, sqlgraph.NewFieldSpec(permissionmenu.FieldID, field.TypeInt64))
	)
	if id, ok := pmc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if nodes := pmc.mutation.PermissionIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   permissionmenu.PermissionTable,
			Columns: []string{permissionmenu.PermissionColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(permission.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.PermissionID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := pmc.mutation.MenuIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   permissionmenu.MenuTable,
			Columns: []string{permissionmenu.MenuColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(menu.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.MenuID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// SetPermissionMenu set the PermissionMenu
func (pmc *PermissionMenuCreate) SetPermissionMenu(input *PermissionMenu, fields ...string) *PermissionMenuCreate {
	m := pmc.mutation
	if len(fields) == 0 {
		fields = permissionmenu.Columns
	}
	_ = m.SetFields(input, fields...)
	return pmc
}

// SetPermissionMenuWithZero set the PermissionMenu
func (pmc *PermissionMenuCreate) SetPermissionMenuWithZero(input *PermissionMenu, fields ...string) *PermissionMenuCreate {
	m := pmc.mutation
	if len(fields) == 0 {
		fields = permissionmenu.Columns
	}
	_ = m.SetFieldsWithZero(input, fields...)
	return pmc
}

// PermissionMenuCreateBulk is the builder for creating many PermissionMenu entities in bulk.
type PermissionMenuCreateBulk struct {
	config
	err      error
	builders []*PermissionMenuCreate
}

// Save creates the PermissionMenu entities in the database.
func (pmcb *PermissionMenuCreateBulk) Save(ctx context.Context) ([]*PermissionMenu, error) {
	if pmcb.err != nil {
		return nil, pmcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(pmcb.builders))
	nodes := make([]*PermissionMenu, len(pmcb.builders))
	mutators := make([]Mutator, len(pmcb.builders))
	for i := range pmcb.builders {
		func(i int, root context.Context) {
			builder := pmcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PermissionMenuMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pmcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pmcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int64(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pmcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pmcb *PermissionMenuCreateBulk) SaveX(ctx context.Context) []*PermissionMenu {
	v, err := pmcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pmcb *PermissionMenuCreateBulk) Exec(ctx context.Context) error {
	_, err := pmcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pmcb *PermissionMenuCreateBulk) ExecX(ctx context.Context) {
	if err := pmcb.Exec(ctx); err != nil {
		panic(err)
	}
}
