// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"origadmin/application/admin/internal/data/entity/ent/permission"
	"origadmin/application/admin/internal/data/entity/ent/predicate"
	"origadmin/application/admin/internal/data/entity/ent/view"
	"origadmin/application/admin/internal/data/entity/ent/viewpermission"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ViewPermissionQuery is the builder for querying ViewPermission entities.
type ViewPermissionQuery struct {
	config
	ctx            *QueryContext
	order          []viewpermission.OrderOption
	inters         []Interceptor
	predicates     []predicate.ViewPermission
	withView       *ViewQuery
	withPermission *PermissionQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ViewPermissionQuery builder.
func (_q *ViewPermissionQuery) Where(ps ...predicate.ViewPermission) *ViewPermissionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ViewPermissionQuery) Limit(limit int) *ViewPermissionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ViewPermissionQuery) Offset(offset int) *ViewPermissionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ViewPermissionQuery) Unique(unique bool) *ViewPermissionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ViewPermissionQuery) Order(o ...viewpermission.OrderOption) *ViewPermissionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryView chains the current query on the "view" edge.
func (_q *ViewPermissionQuery) QueryView() *ViewQuery {
	query := (&ViewClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(viewpermission.Table, viewpermission.FieldID, selector),
			sqlgraph.To(view.Table, view.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, viewpermission.ViewTable, viewpermission.ViewColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPermission chains the current query on the "permission" edge.
func (_q *ViewPermissionQuery) QueryPermission() *PermissionQuery {
	query := (&PermissionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(viewpermission.Table, viewpermission.FieldID, selector),
			sqlgraph.To(permission.Table, permission.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, viewpermission.PermissionTable, viewpermission.PermissionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ViewPermission entity from the query.
// Returns a *NotFoundError when no ViewPermission was found.
func (_q *ViewPermissionQuery) First(ctx context.Context) (*ViewPermission, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{viewpermission.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ViewPermissionQuery) FirstX(ctx context.Context) *ViewPermission {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ViewPermission ID from the query.
// Returns a *NotFoundError when no ViewPermission ID was found.
func (_q *ViewPermissionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{viewpermission.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ViewPermissionQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ViewPermission entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ViewPermission entity is found.
// Returns a *NotFoundError when no ViewPermission entities are found.
func (_q *ViewPermissionQuery) Only(ctx context.Context) (*ViewPermission, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{viewpermission.Label}
	default:
		return nil, &NotSingularError{viewpermission.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ViewPermissionQuery) OnlyX(ctx context.Context) *ViewPermission {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ViewPermission ID in the query.
// Returns a *NotSingularError when more than one ViewPermission ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ViewPermissionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{viewpermission.Label}
	default:
		err = &NotSingularError{viewpermission.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ViewPermissionQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ViewPermissions.
func (_q *ViewPermissionQuery) All(ctx context.Context) ([]*ViewPermission, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ViewPermission, *ViewPermissionQuery]()
	return withInterceptors[[]*ViewPermission](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ViewPermissionQuery) AllX(ctx context.Context) []*ViewPermission {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ViewPermission IDs.
func (_q *ViewPermissionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(viewpermission.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ViewPermissionQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ViewPermissionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ViewPermissionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ViewPermissionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ViewPermissionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ViewPermissionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ViewPermissionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ViewPermissionQuery) Clone() *ViewPermissionQuery {
	if _q == nil {
		return nil
	}
	return &ViewPermissionQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]viewpermission.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.ViewPermission{}, _q.predicates...),
		withView:       _q.withView.Clone(),
		withPermission: _q.withPermission.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithView tells the query-builder to eager-load the nodes that are connected to
// the "view" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ViewPermissionQuery) WithView(opts ...func(*ViewQuery)) *ViewPermissionQuery {
	query := (&ViewClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withView = query
	return _q
}

// WithPermission tells the query-builder to eager-load the nodes that are connected to
// the "permission" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ViewPermissionQuery) WithPermission(opts ...func(*PermissionQuery)) *ViewPermissionQuery {
	query := (&PermissionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPermission = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ViewID int64 `json:"view_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ViewPermission.Query().
//		GroupBy(viewpermission.FieldViewID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ViewPermissionQuery) GroupBy(field string, fields ...string) *ViewPermissionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ViewPermissionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = viewpermission.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ViewID int64 `json:"view_id,omitempty"`
//	}
//
//	client.ViewPermission.Query().
//		Select(viewpermission.FieldViewID).
//		Scan(ctx, &v)
func (_q *ViewPermissionQuery) Select(fields ...string) *ViewPermissionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ViewPermissionSelect{ViewPermissionQuery: _q}
	sbuild.label = viewpermission.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ViewPermissionSelect configured with the given aggregations.
func (_q *ViewPermissionQuery) Aggregate(fns ...AggregateFunc) *ViewPermissionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ViewPermissionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !viewpermission.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ViewPermissionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ViewPermission, error) {
	var (
		nodes       = []*ViewPermission{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withView != nil,
			_q.withPermission != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ViewPermission).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ViewPermission{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withView; query != nil {
		if err := _q.loadView(ctx, query, nodes, nil,
			func(n *ViewPermission, e *View) { n.Edges.View = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPermission; query != nil {
		if err := _q.loadPermission(ctx, query, nodes, nil,
			func(n *ViewPermission, e *Permission) { n.Edges.Permission = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ViewPermissionQuery) loadView(ctx context.Context, query *ViewQuery, nodes []*ViewPermission, init func(*ViewPermission), assign func(*ViewPermission, *View)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ViewPermission)
	for i := range nodes {
		fk := nodes[i].ViewID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(view.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "view_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ViewPermissionQuery) loadPermission(ctx context.Context, query *PermissionQuery, nodes []*ViewPermission, init func(*ViewPermission), assign func(*ViewPermission, *Permission)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ViewPermission)
	for i := range nodes {
		fk := nodes[i].PermissionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(permission.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "permission_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ViewPermissionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ViewPermissionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(viewpermission.Table, viewpermission.Columns, sqlgraph.NewFieldSpec(viewpermission.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, viewpermission.FieldID)
		for i := range fields {
			if fields[i] != viewpermission.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withView != nil {
			_spec.Node.AddColumnOnce(viewpermission.FieldViewID)
		}
		if _q.withPermission != nil {
			_spec.Node.AddColumnOnce(viewpermission.FieldPermissionID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ViewPermissionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(viewpermission.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = viewpermission.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *ViewPermissionQuery) ForUpdate(opts ...sql.LockOption) *ViewPermissionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *ViewPermissionQuery) ForShare(opts ...sql.LockOption) *ViewPermissionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *ViewPermissionQuery) Modify(modifiers ...func(s *sql.Selector)) *ViewPermissionSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// Omit allows the unselect one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
// Example:
//
//	var v []struct {
//	  ViewID int64 `json:"view_id,omitempty"`
//	  PermissionID int64 `json:"permission_id,omitempty"`
//	}
//
//	client.ViewPermission.Query().
//	  Omit(
//	  viewpermission.FieldViewID,
//	  viewpermission.FieldPermissionID,
//	  ).
//	  Scan(ctx, &v)
func (vpq *ViewPermissionQuery) Omit(fields ...string) *ViewPermissionSelect {
	omits := make(map[string]struct{}, len(fields))
	for i := range fields {
		omits[fields[i]] = struct{}{}
	}
	for _, col := range viewpermission.Columns {
		if _, ok := omits[col]; !ok {
			vpq.ctx.Fields = append(vpq.ctx.Fields, col)
		}
	}

	sbuild := &ViewPermissionSelect{ViewPermissionQuery: vpq}
	sbuild.label = viewpermission.Label
	sbuild.flds, sbuild.scan = &vpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// ViewPermissionGroupBy is the group-by builder for ViewPermission entities.
type ViewPermissionGroupBy struct {
	selector
	build *ViewPermissionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ViewPermissionGroupBy) Aggregate(fns ...AggregateFunc) *ViewPermissionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ViewPermissionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ViewPermissionQuery, *ViewPermissionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ViewPermissionGroupBy) sqlScan(ctx context.Context, root *ViewPermissionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ViewPermissionSelect is the builder for selecting fields of ViewPermission entities.
type ViewPermissionSelect struct {
	*ViewPermissionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ViewPermissionSelect) Aggregate(fns ...AggregateFunc) *ViewPermissionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ViewPermissionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ViewPermissionQuery, *ViewPermissionSelect](ctx, _s.ViewPermissionQuery, _s, _s.inters, v)
}

func (_s *ViewPermissionSelect) sqlScan(ctx context.Context, root *ViewPermissionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *ViewPermissionSelect) Modify(modifiers ...func(s *sql.Selector)) *ViewPermissionSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
