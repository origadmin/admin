# databases.yaml
data:
  databases:
    default:
      name: default_db
      dialect: ${DATABASE_DIALECT:-sqlite3} # 从环境变量获取数据库方言，默认值为 sqlite3
      source: ${DATABASE_SOURCE:-file:origadmin.db?cache=shared&_foreign_keys=1} # 从环境变量获取连接字符串
