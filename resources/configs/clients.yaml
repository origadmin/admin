clients:
  configs:
    - name: "origadmin.service.auth.client.grpc"
      grpc:
        endpoint: "discovery:///origadmin.service.auth"
        timeout: 5s
        discovery_name: "default_consul"
    - name: "origadmin.service.system.client.grpc"
      grpc:
        endpoint: "discovery:///origadmin.service.system"
        timeout: 5s
        discovery_name: "default_consul"
